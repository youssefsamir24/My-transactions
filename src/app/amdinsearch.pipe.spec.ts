import { AmdinsearchPipe } from './amdinsearch.pipe';

describe('AmdinsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AmdinsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
